# LinguaFlow environment configuration
# Copy this file to `.env` and fill in your own keys.
# `.env` is git-ignored so your secrets are never committed.

# Primary LLM provider: Alibaba Cloud DashScope (Qwen)
# Get a key at https://dashscope.console.aliyun.com/
QWEN_API_KEY=your_qwen_api_key_here
QWEN_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1

# Model name for the Qwen provider (optional).
# Leave blank to use the default. Examples:
#   qwen-turbo, qwen-plus, qwen-max, qwen3.7-flash-2026-07-15
QWEN_MODEL=qwen3.7-flash-2026-07-15

# Optional fallback provider (used automatically when Qwen hits a quota/rate limit)
# Get a key at https://openrouter.ai/keys
OPENROUTER_API_KEY=

# Model name for the OpenRouter fallback (optional).
# Leave blank to use the default. Examples:
#   google/gemini-2.5-flash, openai/gpt-4o-mini, anthropic/claude-3.5-haiku
OPENROUTER_MODEL=google/gemini-2.5-flash

# --- Speech models (DashScope / Qwen) ---
# TTS 模型：qwen3-tts-flash 一个模型覆盖中/英/日/韩/俄等多语种，浏览器可直接 HTTP 调用。
# ⚠️ 不要改成 sambert-* / cosyvoice-*：那些只支持 WebSocket，普通账号 HTTP 调用会报
#    "current user api does not support http call"，导致朗读静默失败并回落到浏览器机械音。
QWEN_TTS_MODEL=qwen3-tts-flash

# 默认音色（可在 App 内「设置 → 模型设置 → 朗读音色」里切换并试听，界面选择优先于此项）。
# 可选：Cherry / Ethan / Jennifer / Ryan / Elias / Katerina / Nofish / Jada / Dylan / Sunny / Kiki / Rocky
QWEN_TTS_VOICE=Cherry

# STT / ASR model id (optional). Leave blank for default. Examples:
#   paraformer-v2, paraformer-realtime-v2, paraformer-v1
QWEN_STT_MODEL=paraformer-v2

# Recommended primary (free): Zhipu GLM provider (switchable in Settings -> 模型设置)
# Get a key at https://open.bigmodel.cn/ (or your GLM proxy).
# If you use a third-party GLM proxy, set VITE_GLM_BASE_URL to that proxy's
# OpenAI-compatible base URL and VITE_GLM_MODEL to the model id it expects.
# The VITE_ prefix is required so Vite exposes the value to the browser.
VITE_GLM_API_KEY=
VITE_GLM_BASE_URL=https://open.bigmodel.cn/api/paas/v4
VITE_GLM_MODEL=GLM-4.7-Flash
