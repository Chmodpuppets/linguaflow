# LinguaFlow 进度快照（2026-08-27 检查）

> 本文件为只读诊断快照，不修改 site-setup.md / tasklist。PM 中枢的实际刷新需按铁律「先改 site-setup 再建任务」执行。
> 数据来源：git（HEAD=origin/main=324442c）、CHANGELOG.md、constants.tsx、services/aiService.ts、index.html 交叉核对。

## 一、代码 / 版本真实状态（已发布，工作树干净）
- 最新 tag：**v0.1.1**（2026-08-26）；v0.1.0 为功能里程碑（Books + TypeLit）。
- 同步状态：HEAD = origin/main = `324442c`，**ahead 0 / behind 0**，两个 tag 均在远端。
- 最新 commit：`324442c` docs: 移除品牌资源小节（08-26）。
- 导航模块：**20 个**（`constants.tsx` NAV_ITEMS，含 books/typing/song_lab 等）。
- 支持语言：**11 种**（SUPPORTED_LANGUAGES）。
- AI 默认模型：**OpenRouter `stealth/ox-alpha`**（`.env` 可覆盖）；GLM-4.7-Flash 为快模型之一；**Gemini 已迁出**（无 geminiService）。
- TTS 默认模型：**`qwen3-tts-flash`**（覆盖中/英/日/韩/俄多语种）。
- 字体：**已自托管**（index.html 无 Google Fonts 链接）。
- 许可证：**CC BY-NC 4.0**（非商业 + 商用付费授权），v0.1.1 加入。
- README 已拆分为 `docs/`（features/setup/ai-voice/faq/contributing）+ 社群扫码。
- 发版脚本 `scripts/release.sh` 已加（`npm run release`）。

## 二、PM 文档同步性诊断（核心问题：文档停更于 2026-08-18）

| 维度 | site-setup / tasklist 现状 | 代码真实事实 | 落差 |
|---|---|---|---|
| 最后更新 | 2026-08-18 | 2026-08-26（v0.1.1） | 8 天未刷新 |
| 导航模块数 | 19 | **20**（多了 Books） | 少算 1 |
| AI 默认模型 | 默认 GLM-4.7-Flash，兜底 Gemini | 默认 OpenRouter stealth/ox-alpha，Gemini 已迁出 | **全错** |
| TTS 默认 | Sambert（仅中文） | qwen3-tts-flash（多语种） | 已换代 |
| 字体 | “待办：自托管字体” | 已自托管 | 已完成未记 |
| 许可证 | 写 MIT / 未提 | CC BY-NC 4.0 | 已变更未记 |
| README | 单文件长文 | 已拆分 docs/ + 社群扫码 | 大改动未记 |
| release.sh | 无 | 已加 | 未记 |

tasklist.md 的“项目真实进度快照”同样停在 08-18，Task 2–6 仍“待立项”，但 v0.1.0/v0.1.1 的多项交付（Books、TypeLit、License、README 拆分、release 脚本）完全没进任务库。

## 三、待办 / 未立项工作
PM 中枢内仍为提案态：
- **Task 2** 账户级 localStorage 数据导出/导入（一键备份）——未做
- **Task 3** STT CORS 代理开关（DASHSCOPE_PROXY_URL）——未做
- **Task 4** 非中文 TTS CosyVoice 开关——未做（注：qwen3-tts-flash 已多语种，此需求已弱化）
- **Task 5** Playwright 截图回归脚手架——未做
- **Task 6** 走一遍真实 spec→task 样例——未做
- site-setup 第 7 节候选未批准：跨设备同步、UI 自身国际化、AssessmentView 扩展

附带小问题：aiService.ts 第 8、1225–1226 行注释仍写 “sambert-zhide-v1”，与变量（qwen3-tts-flash）不符，可清理。

## 四、建议下一步
1. **刷新 PM 文档**（按铁律先改 site-setup：模块数 19→20、AI 默认模型、TTS 模型、字体自托管、许可证、README 拆分、release.sh；再同步 tasklist 快照到 08-27）。
2. 把 v0.1.0/v0.1.1 已交付项补登 tasklist（作为已关闭条目）。
3. 清理 aiService 过时注释。
4. 确认 Task 2–6 / 候选区的优先级，挑下一项正式立项。
